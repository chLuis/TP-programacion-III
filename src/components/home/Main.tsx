import { PersonalInformation } from ".";

export default function Main({data} : {data: PersonalInformation}) {
  console.log(data);
  return (
    <div className="max-w-7xl px-4 md:px-2 mx-auto">
      
    </div>
  )
}